platform :ios, '9.0'

target 'XCurrency' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for XCurrency
  pod 'ChameleonFramework'
  pod 'SwiftyJSON'

end
